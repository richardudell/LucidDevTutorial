# .env.example — Lucid Developer Tutorial
# =========================================
# Copy this file to .env and fill in your real values.
# The .env file is gitignored — never commit real secrets.
#
# How to use:
#   cp .env.example .env
#   # Edit .env with your real credentials
#
# Both oauth.py and saml.py load these values via python-dotenv.

# ---------------------------------------------------------------------------
# OAuth 2.0 credentials (from the Lucid Developer Portal)
# Get these at: https://developer.lucid.co
# ---------------------------------------------------------------------------

# Your app's unique identifier, issued by the Lucid Developer Portal
LUCID_CLIENT_ID=your_client_id_here

# Your app's secret — treat this like a password, never expose it client-side
LUCID_CLIENT_SECRET=your_client_secret_here

# Where Lucid redirects after the user authorizes.
# Must exactly match what you registered in the Developer Portal.
LUCID_REDIRECT_URI=http://localhost:5000/callback

# ---------------------------------------------------------------------------
# Lucid OAuth endpoints (defaults shown — only override if Lucid changes them)
# ---------------------------------------------------------------------------

# User token authorization — acts on behalf of a single user
LUCID_USER_AUTH_URL=https://lucid.app/oauth2/authorize

# Account token authorization — acts on behalf of the entire organization
LUCID_ACCOUNT_AUTH_URL=https://lucid.app/oauth2/authorizeAccount

# Token endpoint — used for both initial exchange and refresh
LUCID_TOKEN_URL=https://api.lucid.co/oauth2/token

# ---------------------------------------------------------------------------
# Lucid API base URL (all REST calls go here)
# ---------------------------------------------------------------------------

LUCID_API_BASE_URL=https://api.lucid.co

# ---------------------------------------------------------------------------
# SAML settings (used by saml.py)
# These values are provided by your IdP admin during SSO setup.
# ---------------------------------------------------------------------------

# SP Entity ID: a unique URI identifying your app to the IdP.
# Typically the URL of your metadata endpoint.
SAML_SP_ENTITY_ID=http://localhost:5000/saml/metadata

# ACS URL: where the IdP POSTs the SAML response after authentication.
# Register this exact URL with your IdP admin.
SAML_SP_ACS_URL=http://localhost:5000/saml/acs

# IdP Entity ID: the IdP's unique identifier (from their metadata XML)
SAML_IDP_ENTITY_ID=https://your-idp.example.com/saml

# IdP SSO URL: where your app redirects users to authenticate
SAML_IDP_SSO_URL=https://your-idp.example.com/saml/sso

# IdP SLO URL: the IdP's Single Logout endpoint (used for coordinated logout)
SAML_IDP_SLO_URL=https://your-idp.example.com/saml/slo

# IdP signing certificate (PEM format, no headers, no line breaks).
# Get this from the IdP admin or from their metadata XML (<ds:X509Certificate>).
# Example: MIICxDCCAaygAwIBAgIIS...base64content...==
SAML_IDP_CERT=your_idp_x509_certificate_here
